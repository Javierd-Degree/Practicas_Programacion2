#include <stdio.h>
#include <stdlib.h>

#include "node.h"


/*void main(){

    Node *n1, *n2;
    n1 = node_ini();
    node_setName(n1, "topotamadre");
    node_setId(n1, 21);
    n2 = node_copy(n1);
    printf("%d\n", node_equals(n1, n2));
    
    FILE *file = NULL;
    file = fopen("datostwo.txt", "a");
    if(file != NULL) printf("%d", node_print(file, n1));
    fclose(file);
    
}*/
